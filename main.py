

def make_map(level):
    num = level + 2 
    default = [ [0 for _ in range(num)] for _ in range(level)]
    return default


def make_rand_road(grid):
    orientations = ['vertical', 'horizontal', 'neg_perpendicular', 'pos_']
    pass

make_map(3)